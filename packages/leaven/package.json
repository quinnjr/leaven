{
  "name": "@leaven-graphql/leaven",
  "version": "0.2.0",
  "description": "Meta-package for Leaven, a high-performance GraphQL library for the Bun runtime. Re-exports the runtime modules (core, http, ws, context, errors, plugins, playground) and selected schema helpers; the NestJS integration ships separately as @leaven-graphql/nestjs.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "import": "./src/index.ts",
      "types": "./src/index.ts"
    }
  },
  "publishConfig": {
    "access": "public",
    "main": "./dist/index.js",
    "types": "./dist/index.d.ts",
    "exports": {
      ".": {
        "import": "./dist/index.js",
        "types": "./dist/index.d.ts"
      }
    }
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "bun build ./src/index.ts --outdir ./dist --target bun --external @leaven-graphql/* --external graphql && tsc -p tsconfig.build.json",
    "clean": "rm -rf dist",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "keywords": [
    "graphql",
    "bun",
    "api",
    "server",
    "typescript",
    "apollo",
    "mercurius"
  ],
  "author": "Joseph Quinn",
  "license": "Apache-2.0",
  "dependencies": {
    "@leaven-graphql/context": "workspace:*",
    "@leaven-graphql/core": "workspace:*",
    "@leaven-graphql/errors": "workspace:*",
    "@leaven-graphql/http": "workspace:*",
    "@leaven-graphql/playground": "workspace:*",
    "@leaven-graphql/plugins": "workspace:*",
    "@leaven-graphql/schema": "workspace:*",
    "@leaven-graphql/ws": "workspace:*",
    "graphql": "^16.8.0"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "typescript": "^7.0.2"
  },
  "peerDependencies": {
    "graphql": "^16.0.0"
  }
}
